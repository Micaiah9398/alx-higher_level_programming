$(document).ready(function() {
  // When the document is ready, the event listener will be added
  $('#red_header').click(function() {
    // Add the "red" class to the <header> element
    $('header').addClass('red');
  });
});

